/**
 * UserType enum, provides a way to identify different user types.
 */
export enum UserType {
  /**
   * This user is a free user with no personal account.
   */
  Participant = 'Participant',
  /**
   * This is a user with an account.
   */
  User = 'User',
}
